# numpy 1.x: required by the system cv2 / picamera2 on the Pi. numpy 2.x
# breaks the ABI for those wheels — keep this pin.
numpy<2
opencv-python
requests

# Optional — only needed if you run with --voice (Pi joins LiveKit room).
# Heavy install (~30 MB + deps). Install separately:
#   pip install livekit
# livekit
